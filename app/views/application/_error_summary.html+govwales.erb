<% if flash[:validation]&.any? %>
  <div class="govuk-width-container">
    <%= render "govuk_publishing_components/components/error_summary", {
      title: t("coronavirus_form.errors.title"),
      items: flash[:validation].map { |error| 
        {
          text: error[:text],
          href: "#" + error[:field]
        } 
      }
    } %>
  </div>
<% end %>